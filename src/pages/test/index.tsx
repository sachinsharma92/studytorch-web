import React, { useRef, useState } from "react";
import ColorInput from "../../components/colorInput";
import { Popover } from "react-text-selection-popover";

const TestScreen = () => {
  return <ColorInput color="#FF0000" />;
};

export default TestScreen;
